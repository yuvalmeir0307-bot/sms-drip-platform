[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npx tsc --noEmit"

[[services]]
name = "campaign-worker"
startCommand = "npx ts-node --esm workers/campaign-worker.ts"

[[services]]
name = "notion-poller"
startCommand = "npx ts-node --esm workers/notion-poller.ts"
